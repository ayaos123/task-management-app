# 🗂 Task Management Application

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE) 
[![React](https://img.shields.io/badge/React-18.2-blue?logo=react)](https://reactjs.org/) 
[![Laravel](https://img.shields.io/badge/Laravel-10-red?logo=laravel)](https://laravel.com/) 
[![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-3.3-blue?logo=tailwind-css)](https://tailwindcss.com/)

A full-stack **Task Management Application** built with **Laravel (backend)** and **React + TypeScript (frontend)**. It supports user authentication, CRUD operations for tasks, and features a clean, responsive interface.

---

## ✨ Features

### ✅ Backend (Laravel)
- User registration & login with email/password
- JWT-based authentication using **Laravel Sanctum**
- CRUD operations for tasks
- Task status management (`pending`, `in_progress`, `done`)
- User-specific task access control
- RESTful API design
- Input validation & error handling

### ✅ Frontend (React + Vite + TypeScript)
- Login & registration forms
- Protected routes with authentication
- Task list with pagination
- Create, update, delete tasks
- Status management with dropdown
- Responsive design with **Tailwind CSS**
- Loading & error states handling
- Toast notifications

### 🚀 Bonus Features
- Docker setup for easy deployment
- Pagination (10 tasks per page)
- Clean, professional UI
- SQLite database for development

---

## 🛠️ Tech Stack

### Backend
- **Laravel 10.x** (PHP 8.1+)
- **Laravel Sanctum** for authentication
- **SQLite** (development) / **MySQL** (production)
- RESTful API

### Frontend
- **React 18** with TypeScript
- **Vite** build tool
- **Tailwind CSS**
- **React Router DOM**
- **Axios** for API calls
- **React Hot Toast** for notifications

---

## 🐳 Docker Setup (Recommended)

<details>
<summary>Quick Start with Docker</summary>

```bash
# Clone the repo
git clone <your-repo-url>
cd task-management-app

# Copy environment files
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env

# Start Docker containers
docker-compose up -d

# Backend setup
docker-compose exec backend composer install
docker-compose exec backend php artisan key:generate
docker-compose exec backend php artisan migrate

# Frontend: Access at http://localhost:5173
# Backend API: http://localhost:8000

# Stop containers
docker-compose down
</details> <details> <summary>Manual Setup</summary>

Backend

cd backend
composer install
cp .env.example .env
php artisan key:generate
# SQLite DB
touch database/database.sqlite
php artisan migrate
php artisan serve --port=8000


Frontend

cd frontend
npm install
cp .env.example .env
npm run dev

</details>
📡 API Endpoints
Authentication
Method	Endpoint	Description
POST	/api/register	Register new user
POST	/api/login	Login user
POST	/api/logout	Logout user
GET	/api/user	Get authenticated user
Tasks (Protected)
Method	Endpoint	Description
GET	/api/tasks	Get paginated tasks
POST	/api/tasks	Create new task
PUT	/api/tasks/{id}	Update task
DELETE	/api/tasks/{id}	Delete task
🎨 Frontend Structure
Pages

Login - User authentication

Register - New user registration

Dashboard - Task management interface

Components

TaskList - Display tasks with pagination

TaskForm - Create/update tasks

TaskItem - Individual task display

ProtectedRoute - Auth guard

State Management

React Context for authentication

Local state for forms

API state handling (loading, error, success)

🔒 Security

JWT authentication

User-specific authorization

Input validation

Bcrypt password hashing

CORS & CSRF protection

📱 Responsive Design

Mobile-first

Tailwind CSS breakpoints

Accessible forms

📊 Database Schema

Users Table

id, name, email, password, timestamps

Tasks Table

id, user_id, title, description, status, timestamps

📝 Development Workflow

Clone repo → Setup environment → Run Docker or manual setup

Code changes auto-refresh

Run tests before commits

Production build: npm run build + composer install --optimize

🐛 Troubleshooting

Ports in use → change .env or kill process

DB connection errors → check path & permissions

CORS errors → verify frontend API URL & Sanctum config

Docker issues → rebuild containers:

docker-compose down
docker-compose build --no-cache
docker-compose up

🎯 Notes

SQLite for development simplicity

JWT tokens stored in localStorage (demo only)

Production: implement refresh tokens & secure storage

Suitable for small-medium apps

UI is functional; can be improved further


---


